# Weather Fetcher — Core Java

A simple Core Java console application that:

✅ Fetches current currencies data from a public API
✅ Saves the weather info into a CSV file (with timestamp)
✅ Allows the user to enter a currency name in terminal and shows the currency